#include<iostream>
#include<cmath>

int main(){
    //useful math functions
    double x = 3.78;
    double y = 4;
    double z;
    // z = std::max(x,y); this gives us the max of two values
    // z = std::min(x,y); this gives us the min of two values
    // z = pow(2,2); we have power the first number is just a number and the second one how much you want to raise the power
    // z = sqrt(16); we have sqrt to get the sqrt of the number
    // z = round(x); // just rounds to nearest integer
    // z = abs(-19); this give us the positive number
    // z = ceil(x);
    // z = floor(x); floor just reduces the number
    std::cout<<z;
    return 0;
}